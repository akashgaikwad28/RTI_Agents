"use client";

export function ReflectionLoopView() {
  return <div className="rounded-lg border border-[hsl(var(--border))] bg-[hsl(var(--surface))] p-4 text-sm">Reflection retries are shown here when reviewer or human approval fails.</div>;
}

