import LoginPage from "@/app/login/page";

// /register serves the same unified auth page but starts in register mode.
// The LoginPage component reads the pathname to set initial mode.
export default LoginPage;
